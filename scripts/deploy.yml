---
- name: Deploy
  hosts: all
  connection: local
  gather_facts: no
  tasks:
    - name: Start Ubuntu Containers
      docker_container: 
          name: "{{containerImage}}"
          image: "{{containerImage}}"
          pull: yes
          recreate: yes
          state: started
