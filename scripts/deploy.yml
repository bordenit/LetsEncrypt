---
- name: Deploy
  hosts: all
  connection: local
  gather_facts: no
  tasks:
    - name: Install PIP module
      pip:
        name: docker

    - name: Start Ubuntu Containers
      docker_container: 
          name: "{{containerName}}"
          image: "{{containerImage}}"
          pull: yes
          recreate: yes
          state: started
